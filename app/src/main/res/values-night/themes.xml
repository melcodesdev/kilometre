<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark-mode window theme. The system swaps this in when the device is
         in night mode, so the Activity's resources config carries the night
         bit and Compose's isSystemInDarkTheme() resolves to dark. -->
    <style name="Theme.Kilometre" parent="Theme.AppCompat.NoActionBar" />
</resources>
