<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <!-- North half: red, so the tip reads as north when the needle rotates
         with the map. South half: white to match the other map glyphs. The
         needle is drawn untinted (Icon tint = Unspecified) so both colours
         survive on the translucent button. -->
    <path
        android:fillColor="#E53935"
        android:pathData="M12,3 L9.5,12 L14.5,12 Z" />
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M12,21 L9.5,12 L14.5,12 Z" />
</vector>
